<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Promo Ad</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@700;800;900&display=swap" rel="stylesheet" />
<style>
  :root{
    --yellow:#FFC300;
    --black:#000000;
    --white:#ffffff;
  }
  *{box-sizing:border-box}
  body{
    margin:0;
    background:#eaeaea;
    font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
  }
  .ad{
    position:relative;
    width:1080px;
    height:1080px;
    margin:24px auto;
    background:var(--yellow);
    overflow:hidden;
  }

  /* Headline */
  .headline{
    position:absolute;
    top:96px;
    left:64px;
    color:var(--black);
    font-weight:900;
    font-size:150px;
    line-height:0.9;
    letter-spacing:-2px;
    text-transform:uppercase;
  }

  /* Discount badge */
  .badge{
    position:absolute;
    top:128px;
    right:96px;
    width:280px;
    height:280px;
    border-radius:50%;
    background:var(--white);
    display:flex;
    align-items:center;
    justify-content:center;
    text-align:center;
    color:var(--black);
    font-weight:900;
    line-height:1;
  }
  .badge .percent{
    font-size:88px;
  }
  .badge .off{
    font-size:80px;
  }

  /* Product */
  .product{
    position:absolute;
    left:120px;
    right:120px;
    top:420px;
    bottom:240px;
    display:flex;
    align-items:center;
    justify-content:center;
  }
  .product img{
    max-width:100%;
    max-height:100%;
    object-fit:contain;
    filter: drop-shadow(0 28px 24px rgba(0,0,0,0.25));
  }

  /* Footer row */
  .footer{
    position:absolute;
    left:64px;
    right:64px;
    bottom:88px;
    display:flex;
    align-items:center;
    justify-content:space-between;
  }
  .brand{
    color:var(--black);
    font-weight:900;
    font-size:88px;
    letter-spacing:-1px;
  }
  .cta{
    background:var(--black);
    color:var(--white);
    font-weight:800;
    font-size:56px;
    padding:28px 56px;
    border-radius:80px;
    text-decoration:none;
  }

  /* Optional: responsive scale for previewing smaller screens */
  @media (max-width:1100px){
    .scale-wrap{transform:scale(0.6); transform-origin:top left; height:648px}
    body{display:flex; justify-content:center}
  }
</style>
</head>
<body>
  <div class="scale-wrap">
    <section class="ad" role="img" aria-label="Promotional ad with discount and product">
      <h1 class="headline">SPORT<br/>SHOES</h1>

      <div class="badge" aria-hidden="true">
        <div>
          <div class="percent">10%</div>
          <div class="off">OFF</div>
        </div>
      </div>

      <div class="product">
        <!-- Replace src with your image path or URL -->
        <img src="product.jpg" alt="Featured product" />
      </div>

      <div class="footer">
        <div class="brand">ON<span style="margin:0 18px">•</span>STORE</div>
        <a class="cta" href="#" aria-label="Get yours">Get yours</a>
      </div>
    </section>
  </div>
</body>
</html>